apiVersion: v1
kind: Service
metadata:
  name: mock-api-service
  namespace: application-ns

spec:
  type: LoadBalancer

  selector:
    app: mock-api

  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP